#include "dkli.dkh"
#!
module "form.dk"
{
    #include "functions.dkh"
    #include "serialize.dkh"
    #include "uielements.v12.dkl"
    #include "../_lib/config.dk"
    #include "../_lib/lib.dk"
	#include "../_lib/icons.dk"

    do @context.begin(@crud_context)

    ref entity = ifnull(@("&./"),record.create())
    
	@_entities_type = @("$parameters/_entities_type")
	@_entity_id = @("$parameters/_entity_id")

	is_new = (@_entity_id == "_new")

    header_act::
    { 
        #$
        button(type="submit" class="btn btn-sm border-secondary induxsoft-corners btn-main-action no-print"){$"#<@ICON_SAVE>"}
        #!
    }
    point general_act to header_act

    new nav_config
    {
        @"title": "Configuración de plantilla"
        @"buttons*": general_act
        @"urlexit": "/!/dbext/globalvar/"
        member @"hidden" 
        { 
            @"print": @true 
            @"send":@true
            @"paste":@true
            @"copy":@true
            @"csv":@true
        }
    }

    #$ div(class="container")
    {
        form(method="POST" action="./" id="form")
        {
            #! do uie.printV12FormBar(nav_config)

            #$ div(id="work_area" class="container-section")
            {
                label(class="fw-bold" "for"=""){"Plantilla:"}

                textarea(class="induxsoft-form-control" name="template" id="" rows="10"){$"#<@@(entity,'$template')>"}
            }
        }
    }
}